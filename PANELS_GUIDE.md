# راهنمای اتصال پنل‌ها به ربات

ربات از دو نوع پنل پشتیبانی می‌کند: **3x-ui** و **Marzban**. می‌توانی هر تعداد پنل (از هر نوع) متصل کنی؛ ربات هنگام ساخت کانفیگ بین پنل‌های فعال به‌صورت چرخشی تقسیم می‌کند و اگر یکی در دسترس نباشد خودکار سراغ بعدی می‌رود.

## ورود به داشبورد

توی ربات، دکمه «🖥 مدیریت پنل‌ها» را بزن (فقط برای ادمین نمایش داده می‌شود) یا دستور `/panels` را ارسال کن.

---

## اتصال پنل 3x-ui

### ۱. آدرس Base URL
آدرس پنل با پورت، مثلاً:
```
https://panel.example.com:2053
```
- اگر SSL نداری `http://` بگذار.
- این همان آدرسی است که با آن وارد پنل می‌شوی.

### ۲. یوزرنیم و رمز عبور
همان اطلاعات ورود ادمین پنل.

### ۳. شماره Inbound ID
- در پنل به تب **Inbounds** برو.
- کنار هر اینباند یک ستون **ID** هست (عددی مثل 1 یا 2).
- کانفیگ‌های ربات روی همین اینباند ساخته می‌شوند. مطمئن شو آن اینباند فعال است و پروتکل VLESS/VMess دارد.

### ۴. آدرس Subscription (Sub Base URL)
این آدرس جایی است که کاربر لینک اشتراکش را از آن دریافت می‌کند:
1. در 3x-ui به **Panel Settings → Subscription** برو.
2. گزینه **Enable Subscription** باید فعال باشد.
3. مقدار **Subscription Port** و اگر داری **Subscription TLS Domain/Certificate** را ببین.
4. آدرس نهایی به این شکل است: `https://دامنه-یا-IP:پورت-سبسکریپشن`

مثال: `https://sub.example.com:2096`

> ⚠️ اگر از IP مستقیم استفاده می‌کنی و SSL داری ممکن است مرورگر اخطار بدهد؛ بهترین کار اتصال یک دامنه/ساب‌دامنه به سرور است.

### تست اتصال
بعد از ذخیره، ربات خودش لاگین را تست می‌کند. اگر خطا گرفتی، از لیست پنل‌ها روی پنل بزن و «🔌 تست اتصال» را امتحان کن.

---

## اتصال پنل Marzban

### ۱. آدرس Base URL
آدرس داشبورد Marzban، مثلاً:
```
https://marzban.example.com:8000
```

### ۲. یوزرنیم و رمز عبور
اطلاعات اکانت ادمین (superadmin) که با آن وارد داشبورد می‌شوی.

### ۳. آدرس Subscription
در فایل `.env` سرور Marzban این متغیر را پیدا کن:
```
XRAY_SUBSCRIPTION_URL_PREFIX = "https://sub.example.com"
```
همان مقدار را به ربات بده. اگر خالی بود، آدرس خود پنل + پورت را امتحان کن.

لینک اشتراک کاربران به شکل `https://{sub_base_url}/sub/{username}/` ساخته می‌شود.

> نکته: ربات برای هر سفارش یک کاربر جدید با پروکسی **VLESS** در Marzban می‌سازد.

---

## مدیریت چند پنل

| عملیات | روش |
|---|---|
| مشاهده لیست | `/panels` |
| جزئیات و مدیریت | روی نام پنل بزن |
| فعال/غیرفعال کردن | دکمه 🚫 / ✅ در صفحه پنل |
| تست اتصال | دکمه 🔌 — لاگین واقعی انجام می‌شود |
| حذف | دکمه 🗑 با تأیید دومرحله‌ای |

### توزیع بار (Round-Robin)
ربات سفارش‌ها را یکی‌درمیان بین پنل‌های فعال تقسیم می‌کند. اگر یک پنل موقع ساخت کانفیگ خطا بدهد (قطعی، پسورد عوض‌شده و…)، ربات **بدون توقف** سراغ پنل بعدی می‌رود تا سفارش کاربر تحویل شود. اگر همه پنل‌ها شکست بخورند، پیام خطا برای ادمین می‌آید.

> پیشنهاد: پنلی که موقتاً از سرویس خارج می‌شود را غیرفعال کن تا سرعتی بالاتر برود.
